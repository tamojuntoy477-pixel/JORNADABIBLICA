name: Build NOX Dragon Arena APK

on:
  push:
    branches: [nox-dragon-arena-apk]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'
      - uses: android-actions/setup-android@v3
      - name: Install Android SDK
        run: |
          yes | sdkmanager --licenses >/dev/null || true
          sdkmanager "platforms;android-35" "build-tools;35.0.0"
      - uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: '8.10.2'
      - name: Build APK
        run: cd noxapp && gradle assembleDebug --no-daemon
      - name: Rename APK
        run: cp noxapp/app/build/outputs/apk/debug/app-debug.apk NOX-Dragon-Arena-3D.apk
      - uses: actions/upload-artifact@v4
        with:
          name: NOX-Dragon-Arena-3D-APK
          path: NOX-Dragon-Arena-3D.apk
