plugins {
    id 'com.android.application'
}

android {
    namespace 'com.nox.formularivals'
    compileSdk 35

    defaultConfig {
        applicationId 'com.nox.formularivals'
        minSdk 23
        targetSdk 35
        versionCode 1
        versionName '1.0'
    }

    buildTypes {
        release {
            minifyEnabled false
        }
    }
}
